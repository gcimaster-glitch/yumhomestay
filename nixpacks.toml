[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@10.4.1 && node $(npm root -g)/pnpm/bin/pnpm.cjs install --frozen-lockfile"]

[phases.build]
cmds = ["node $(npm root -g)/pnpm/bin/pnpm.cjs run build"]

[start]
cmd = "node dist/index.js"
